[package]
name = "herdr-tab-title"
version = "0.1.0"
edition = "2024"

[dependencies]
herdr-plugin = "0.1.4"
regex = "1"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
